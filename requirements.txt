# Dependencies are now managed in pyproject.toml.
# Use uv to install:
#
#   uv sync                              # core deps only
#   uv sync --extra brokers              # + Interactive Brokers
#   uv sync --extra db                   # + PostgreSQL / Redis
#   uv sync --extra ta                   # + technical-analysis libs (needs: brew install ta-lib)
#   uv sync --extra ml                   # + machine-learning / optimisation
#   uv sync --extra brokers --extra ml   # combine extras freely
#   uv sync --extra dev                  # + linting / testing tools
#
# This file is kept only for tools that expect requirements.txt.
# It is NOT the canonical dependency list.
